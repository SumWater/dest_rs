# 当前实验意图与结果总览

本文档用于向后续继续思考方案的模型或研究者说明：我们现在到底在解决什么问题，已经做过哪些实验，结果说明了什么，以及哪些方向已经基本不值得继续投入。

## 1. 任务与核心矛盾

任务是策略可控的谈判对话生成：

> 给定对话上下文和 9 种谈判策略之一，生成符合目标策略的回复。

当前模型框架是：

- `Prefix`：每种策略一组独立 Prefix，用于策略控制。
- `LoRA`：所有策略共享，用于提升话术流畅性和降低 PPL。
- 基座模型 frozen，训练资源有限，主要在单卡或双卡 48G/80G 环境中运行。

核心矛盾是：

```text
Prefix-only 策略控制较好，但 PPL 较差；
Prefix + LoRA PPL 明显改善，但策略准确率显著下降。
```

关键 baseline：

| 实验 | 含义 | 策略准确率 | PPL 情况 |
|---|---|---:|---|
| B3 | Prefix-only | 55.93% | PPL 高，约 10+ |
| B4 | Prefix + LoRA 联合训练 | 37.17% | PPL 好，约 8 |
| B9 | 先训 Prefix，冻住 Prefix，再训 LoRA | 38.89% | PPL 好，约 8 |

B9 是重要证据：Prefix 权重不变时，LoRA 仍然让控制能力下降。这说明问题不只是训练时梯度冲突，也可能是 LoRA 改变了 Prefix 工作的模型环境。

## 2. 已确认的诊断结论

### 2.1 标准 LM loss 对策略身份不敏感

之前做过对比生成 loss：

> 正确 Prefix 的 token-level LM loss 应低于错误 Prefix。

但实验中 contrastive loss 基本长期为 0，说明标准 token-level LM loss 对策略身份差异并不敏感。换错 Prefix 后，只要回复仍然流畅、符合上下文，LM loss 可能几乎不变。

这点非常关键：如果训练目标本身不惩罚“策略错了但话很顺”，那么内部表示或参数空间的修补不一定能反映到最终输出行为。

### 2.2 表示空间分离不等于功能分离

已经尝试过 hidden-state 层面的正交约束、辅助分类 loss、梯度路由等方法。它们最多能在中间表示上制造某种可分性，但没有稳定恢复最终生成的策略可控性。

当前更准确的判断是：

> 参数空间或表示空间的分离，不保证输出端功能可控。

## 3. `solutions/` 方向的实验意图

`solutions/` 目录尝试的是“通道保护 / LoRA 环境适应”方向。它背后的假设是：

> LoRA 修改了 attention 的 Q/K/V/O 投影，导致 Prefix 发出的策略信号在穿过修改后的 attention 空间时被扭曲或淹没。只要让 Prefix 适应 LoRA 空间，或保护 Prefix 相关通道，就可能恢复策略控制。

这个方向分成几类：

### S1：Reverse Curriculum

意图：

1. 先训练 LoRA，让模型获得流畅谈判回复能力。
2. 再冻结 LoRA，只训练 Prefix。
3. 让 Prefix 从一开始就在 LoRA 修改后的 attention 空间中学习策略控制。

想验证的问题：

> 如果 Prefix 不是先在原始基座空间里学习，而是在固定 LoRA 空间中重新学习，是否能恢复控制能力？

### S2：Parameter-Level Orthogonality

意图：

在参数交互层面约束 LoRA，避免 LoRA 破坏 Prefix 通道。

重点是 QK 约束：

```text
约束 LoRA Query 更新不要过度读取 Prefix key 子空间。
```

直觉上，这是在 attention 内部为 Prefix 保留一条更干净的策略通道。

### S3：Attention Analysis

意图：

比较 B3 和 B9 中模型对 Prefix token 的 attention 分布，判断 LoRA 是否压缩、重分配或放大了 Prefix attention。

目前没有形成有效结论。

### S4：Contrastive Prefix Decoding

意图：

推理时计算：

```text
final_logits = target_logits + alpha * (target_logits - neutral_logits)
```

希望减去通用流畅度成分，放大目标 Prefix 的策略差异。

旧版 S4 曾生成大量 `!!!!`，后来定位为 `temperature=0.0` 时 logits 除以 0 的实现 bug。因此旧 S4 结果不能作为方法结论。

## 4. `solutions/` 实验结果

### 4.1 S1 是有效负结果

S1 Stage 2 的配置已检查：

```json
{
  "warm_start_lora": true,
  "warm_start_prefix": false,
  "enable_lora": true,
  "train_prefix": true,
  "train_lora": false,
  "inject_strategy_text": false
}
```

这说明它确实是加载 Stage 1 LoRA，冻结 LoRA，只训练 Prefix。

结果：

```text
S1 accuracy = 15.93%
```

预测分布严重塌缩：

```text
self-need: 119 / 270
small-talk: 98 / 270
```

结论：

> 让 Prefix 在固定 LoRA 空间中重新学习，并不能恢复策略控制。标准 LM loss 仍然会把 Prefix 推向高频、低风险、通用的谈判回复模式。

### 4.2 S2_qk 修复后也是有效负结果

S2 初始版本有代码问题：4-bit base weight 处理错误，导致参数正交 loss 全部 skip。因此旧 S2 结果不能作为方法结论。

修复后，S2_qk v3 的诊断指标是：

```text
param_orth_avg_n_qk = 36.0
param_orth_avg_skipped_qk = 0.0
skip_shape = 0.0
skip_gqa = 0.0
train_param_orth_qk > 0
```

这说明 QK 参数正交真实接入了 36 层，没有再被跳过。

结果：

```text
S2_qk v3 accuracy = 27.41%
```

对比：

| 实验 | 策略准确率 |
|---|---:|
| B3 Prefix-only | 55.93% |
| B4 Prefix+LoRA | 37.17% |
| B9 Prefix-then-LoRA | 38.89% |
| S2_qk v3 | 27.41% |

S2_qk v3 的类别表现：

| 策略 | 准确率 |
|---|---:|
| elicit-pref | 46.67% |
| no-need | 16.67% |
| other-need | 6.67% |
| promote-coordination | 6.67% |
| self-need | 83.33% |
| showing-empathy | 36.67% |
| small-talk | 40.00% |
| uv-part | 0.00% |
| vouch-fair | 6.67% |

预测分布仍然集中：

```text
self-need: 122
small-talk: 63
```

结论：

> QK 参数正交真实生效后，没有恢复策略控制，反而比 B4 更差。保护 Prefix-Key 通道不是有效修复方向。

### 4.3 S4 旧结果无效

旧 S4 生成 `!!!!`，原因是实现 bug：

```text
temperature=0.0 时 logits 被除以 0。
```

该 bug 已修复，但修复后的 S4 尚未完整重跑，因此暂时不能把 S4 写成有效负结果。

### 4.4 S3 暂无有效结论

目前没有可用的 attention analysis 输出，不能支撑论文结论。

## 5. 评估器与数据标签问题

我们做了 gold utterance judge calibration：把真实数据中的 gold 回复交给同一个 LLM judge 判断策略。

结果：

```text
Gold judge accuracy = 54.35%
```

按类别：

| 策略 | Gold judge 准确率 |
|---|---:|
| elicit-pref | 78.95% |
| self-need | 71.79% |
| small-talk | 68.60% |
| uv-part | 50.00% |
| no-need | 33.33% |
| showing-empathy | 30.00% |
| vouch-fair | 30.00% |
| promote-coordination | 22.86% |
| other-need | 18.18% |

说明：

1. 策略标签和 LLM judge 之间存在明显不一致。
2. `other-need`、`promote-coordination`、`vouch-fair`、`showing-empathy` 等策略边界模糊。
3. 自动策略准确率不能被当作绝对真值。

但这不能完全解释 Prefix+LoRA trade-off，因为：

```text
B3 Prefix-only = 55.93%
Gold judge = 54.35%
```

B3 已经接近 gold judge 校准水平，而 B4、B9、S1、S2_qk v3 都明显更低。因此 trade-off 不是纯评估噪声。

## 6. 当前总体结论

现在最重要的结论是：

> 问题不只是 LoRA 修改了 attention 通道，导致 Prefix 信号穿不过去。更根本的问题是：标准 LM loss 对策略身份不敏感；LoRA 提升流畅度后，模型更倾向于生成高概率、通用、低风险的回复，而 Prefix 差异没有足够训练压力稳定反映到最终输出。

换句话说：

```text
旧方向是在修“信号通道”；
真正需要优化的是“输出行为”。
```

`solutions/` 的负结果支持以下判断：

1. 让 Prefix 适应 LoRA 空间不够。
2. 保护 Prefix-Key 参数通道不够。
3. 中间表示或参数空间可分，不等于最终生成行为可控。
4. 需要直接对最终输出的策略符合度施加训练信号。

## 7. 不建议继续投入的方向

不建议继续主攻：

```text
Prefix 通道保护
attention 内部正交
hidden-state 几何分离
仅靠标准 LM loss 的 Prefix 重训练
```

这些方向可以作为论文负结果和诊断证据，但不太可能成为最终有效方法。

## 8. 建议 GPT-5.6 思考的新方向

下一步应该转向 output-level functional control，即直接优化最终回复是否符合策略。

可以让 GPT-5.6 重点思考以下方向：

### 8.1 Strategy-Contrastive Preference Optimization

基本思路：

1. 固定一个 PPL 好的模型，例如 B4 或 B9。
2. 对同一个 context，用 9 个 Prefix 生成 9 个回复。
3. 用 LLM judge 或人工规则筛选：
   - chosen：被判为目标策略的回复；
   - rejected：被判为错误策略的回复。
4. 冻结 LoRA，只微调 Prefix。
5. 用 DPO / pairwise ranking / hinge preference loss 训练：

```text
score(context, target_strategy, chosen) >
score(context, target_strategy, rejected)
```

这样训练目标直接作用于输出行为，而不是期待 token-level LM loss 自然区分策略。

### 8.2 Judge-in-the-loop Prefix Refinement

使用 LLM judge 或轻量策略分类器作为 reward，对 Prefix 进行强化或偏好优化。关键是控制成本和 reward 噪声。

### 8.3 Strategy Verbalizer + Soft Prefix Hybrid

既然纯 soft Prefix 容易被 LM loss 淹没，可以考虑把策略用短文本 verbalizer 显式注入，同时用 Prefix 学习风格细节。需要注意公平比较：显式文本指令会改变任务设定。

### 8.4 Constrained Decoding / Reranking

生成多个候选，用策略 judge rerank。优点是训练成本低，缺点是推理成本高，且依赖 judge 可靠性。

### 8.5 Data-side Reframing

当前策略标签边界模糊。可以考虑：

- 合并模糊策略；
- 使用粗粒度策略组；
- 为每类策略构造更显式的 synthetic contrast pairs；
- 从“生成 gold utterance”改成“生成符合策略定义的可接受 utterance”。

## 9. 给后续模型的关键提示

请不要再默认假设：

> 只要内部 Prefix 表示保留下来，输出就会可控。

已有实验显示这个假设不成立。

更有希望的假设是：

> 必须给最终输出行为本身施加策略约束，尤其要让模型看到“同一上下文下，不同策略输出应该不同”的偏好或对比信号。

后续方案最好满足：

1. 不显著恶化 PPL。
2. 不需要同时运行多个 8B 模型。
3. 能在单卡 48G/80G 上训练或离线构造数据。
4. 能直接提升最终生成回复的策略可判别性。
5. 能处理 LLM judge 噪声，最好有人工检查或校准机制。

