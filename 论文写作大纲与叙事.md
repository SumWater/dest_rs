# 论文写作大纲与叙事

> 最后更新：2026-06-24  
> 用途：本文档面向论文写作，不是实验台账。它把 `实验完整记录.md` 中的事实结果整理成可用于网页端 AI 辅助写论文的主线、章节结构、贡献点、结果组织方式和写作提示。  
> 建议使用方式：把本文档连同 `实验完整记录.md` 一起发给任意网页端 AI，让它先理解论文定位，再按章节生成中文或英文初稿。

---

## 0. 一句话论文定位

本文不再主张“DeST-RS 完整方法显著优于所有基线”，而应定位为：

> **Prefix-LoRA 混合 PEFT 在谈判策略可控生成中的诊断分析研究：系统比较 Prefix、LoRA 及其混合变体，揭示策略控制与生成质量之间的 trade-off，并分析简单正交约束、分类辅助监督、梯度路由和对比损失为何不能稳定消除 Prefix-LoRA 干扰。**

更短的英文定位：

> A diagnostic study of Prefix-LoRA hybrid PEFT for strategy-controlled negotiation dialogue generation, focusing on control-quality trade-offs and interference between strategy-specific Prefix and shared LoRA adaptation.

---

## 1. 推荐题目

### 1.1 英文题目候选

1. **When Prefix Meets LoRA: Diagnosing Hybrid PEFT Interference in Strategy-Controlled Negotiation Dialogue Generation**
2. **Understanding Control-Quality Trade-offs in Hybrid PEFT for Strategy-Controlled Negotiation Dialogue**
3. **A Diagnostic Study of Prefix-LoRA Adaptation for Strategy-Controlled Negotiation Dialogue Generation**
4. **Prefix or LoRA? Diagnosing Parameter-Efficient Adaptation for Negotiation Strategy Control**

### 1.2 中文题目候选

1. **面向谈判策略可控生成的 Prefix-LoRA 混合 PEFT 干扰机制诊断研究**
2. **Prefix 与 LoRA 在谈判策略可控生成中的功能分工与控制-质量折中分析**
3. **混合参数高效微调在谈判策略生成中的控制退化与诊断分析**

### 1.3 推荐最终题目

如果投稿英文会议，建议用：

> **When Prefix Meets LoRA: Diagnosing Hybrid PEFT Interference in Strategy-Controlled Negotiation Dialogue Generation**

这个题目优点：
- 明确有 Prefix 和 LoRA；
- 明确是 diagnostic study，不承诺方法 SOTA；
- 明确任务是 strategy-controlled negotiation dialogue generation；
- 能解释为什么完整方法没有最好：论文目标本来就是诊断干扰。

---

## 2. 论文核心问题与研究问题（Research Questions）

论文应围绕以下研究问题组织，而不是按实验发生顺序写流水账。

### RQ1：Prefix 和 LoRA 在策略可控生成中分别承担什么功能？

对应实验：
- B1 Frozen Base + strategy text
- 旧 B2 LoRA-only（无策略文本）
- 新 B2_text_lora（LoRA + strategy text）
- B3 Prefix-only

核心发现：
- 文本策略指令能带来一定控制能力，但有限。
- 无策略输入的 LoRA 不能做策略控制。
- 加入显式策略文本后，LoRA 可以获得一定策略控制能力。
- Prefix-only 在当前设置下仍然最强，说明策略专属 Prefix 更适合承载细粒度 strategy steering。

论文表述：

> Strategy-specific Prefix provides the strongest controllability under our setting, while LoRA mainly improves language modeling adaptation unless explicit strategy text is injected.

---

### RQ2：Prefix + LoRA 联合训练能否自然实现“Prefix 管策略、LoRA 管生成”的功能分工？

对应实验：
- B3 Prefix-only
- B4 Prefix+LoRA

核心发现：
- B4 的 PPL 明显低于 B3，说明 LoRA 改善生成适配。
- 但 B4 的策略准确率低于 B3，说明 LoRA 加入后削弱策略控制。
- 因此简单联合训练没有自然形成理想功能分工，而是产生 control-quality trade-off。

论文表述：

> Joint Prefix-LoRA tuning improves perplexity but degrades strategy controllability, indicating that shared LoRA adaptation interferes with the fine-grained steering signal learned by strategy-specific Prefix.

---

### RQ3：正交约束和分类辅助监督能否缓解 Prefix-LoRA 干扰？

对应实验：
- B5 Prefix+LoRA+Orth
- 原始 B6 DeST-RS（含 bug，作为内部开发记录，不建议作为主结果）
- B6_fix
- B6_cls_5.0
- B6_orth_1.0

核心发现：
- 原始 B5 的正交约束过弱，策略准确率反而下降。
- 修复 B6 的梯度泄漏 bug 后，训练稳定，但策略控制仍然不强。
- 增强 λ_cls 或 λ_orth 只能带来有限改善，仍不能恢复 Prefix-only 的控制能力。
- 分类头能快速学会内部表示分类，但这种内部可分性没有充分转化为生成文本的策略一致性。
- 几何正交不等于功能解耦。

论文表述：

> Orthogonal regularization and auxiliary classification improve optimization signals in representation space, but they do not reliably translate into better strategy realization in generated utterances.

---

### RQ4：干扰是否主要来自梯度冲突或损失设计？

对应实验：
- B6_grad
- B6_grad_no_orth
- B6_contrastive
- B6_contrastive_no_orth

核心发现：
- 梯度路由相比 B6_fix 有改善，但仍低于 Prefix-only 和部分联合训练基线。
- 去掉 orth 后，梯度路由变得不稳定，说明 orth 更像协调信号，而不是充分的解耦机制。
- contrastive_loss 全程接近 0，说明替换错误策略 Prefix 并不会显著增加 token-level generation loss。
- 这表明普通 token-level LM loss 对策略差异不敏感，大量 token 与策略无关。

论文表述：

> Gradient conflict explains part of the degradation, but not all of it. Moreover, token-level generation loss is insufficiently sensitive to strategy changes, as replacing the target Prefix often leaves the generation loss nearly unchanged.

---

### RQ5：两阶段训练或 warm-start 是否能提供更好的控制-质量折中？

对应实验：
- B7 warm-start DeST-RS / B7_ep2
- B9 Prefix→LoRA

核心发现：
- 旧修复组中，B7/B9 在 PPL 和策略准确率之间取得较好折中。
- 它们策略准确率低于 B3，但 PPL 显著优于 B3。
- 新主线单次结果中 B7/B9 未稳定超过 B2_text_lora 或 B4，因此论文中必须谨慎。
- 当前最稳写法：两阶段和 warm-start 是 promising trade-off，不是已证明稳定最优。

论文表述：

> Warm-start and staged Prefix→LoRA training show promise as practical trade-off strategies, preserving part of Prefix controllability while achieving LoRA-level perplexity. However, their advantage should be interpreted cautiously and validated across seeds.

---

### RQ6：LLM-as-judge 策略评估是否可靠？

对应实验：
- `scripts/calibrate_evaluator.py`
- 人工验证集回复上的 LLM judge 校准

核心发现：
- LLM judge 对人工文本的策略分类准确率约 52.0%。
- no-need、other-need、uv-part 等类别可靠性较差。
- 模型生成文本可能利用 shortcut，让 judge 更容易判对。
- 因此不能只报 overall accuracy，必须报告校准、per-class breakdown、混淆和人工 sanity check。

论文表述：

> Calibration on human-written responses reveals substantial ambiguity and evaluator bias in LLM-based strategy classification. Therefore, automatic strategy accuracy should be interpreted together with calibration and class-level analysis.

---

## 3. 推荐贡献点写法

论文贡献不要写“提出一个效果最强的新模型”。建议写成以下四点。

### Contribution 1：系统诊断 Prefix 与 LoRA 的功能分工

英文：

> We present a systematic diagnostic study of strategy-specific Prefix and shared LoRA adaptation for strategy-controlled negotiation dialogue generation, comparing text prompting, LoRA-only adaptation, Prefix-only tuning, joint Prefix-LoRA tuning, and multiple hybrid variants.

中文：

> 本文系统诊断了策略专属 Prefix 与共享 LoRA 在谈判策略可控生成中的功能分工，比较了文本指令、LoRA、Prefix、Prefix+LoRA 及多种混合变体。

---

### Contribution 2：揭示控制-质量 trade-off

英文：

> We show that Prefix-only tuning yields stronger fine-grained strategy control, whereas LoRA substantially improves perplexity; however, naively combining them introduces a control-quality trade-off.

中文：

> 实验表明，Prefix 更适合细粒度策略控制，LoRA 更适合降低 PPL 与提升生成适配，但简单联合训练会带来策略控制与生成质量之间的折中。

---

### Contribution 3：分析常见缓解机制的局限

英文：

> Through orthogonal regularization, auxiliary classification, gradient routing, and contrastive generation objectives, we demonstrate that neither geometric separation nor auxiliary supervision fully resolves Prefix-LoRA interference.

中文：

> 通过正交约束、分类辅助监督、梯度路由和对比生成损失等诊断实验，本文发现简单几何解耦和辅助监督并不能稳定消除 Prefix-LoRA 干扰。

---

### Contribution 4：校准 LLM-as-judge 并指出评估风险

英文：

> We calibrate the LLM-based strategy evaluator on human-written responses and identify substantial class ambiguity and shortcut risks, highlighting the need for cautious interpretation of automatic strategy accuracy.

中文：

> 本文对 LLM-based 策略评估器进行了校准，发现其在人类回复上的分类准确率有限，且存在类别混淆和 shortcut 风险，从而强调自动策略准确率需要谨慎解释。

---

### Contribution 5（可选）：两阶段训练作为实用折中

如果最终多 seed 支持 B7/B9，可以加入：

> We further show that warm-start and staged Prefix→LoRA training can serve as practical trade-off strategies, preserving part of Prefix controllability while benefiting from LoRA's generation adaptation.

如果多 seed 不稳定，则把它放到 analysis/discussion，不作为主要贡献。

---

## 4. 推荐摘要逻辑

### 4.1 中文摘要草稿

谈判对话生成不仅要求回复自然流畅，还需要符合特定谈判策略。参数高效微调为大语言模型的策略可控生成提供了有效手段，但不同 PEFT 模块在策略控制与生成适配中的功能分工尚不清楚。本文围绕 CaSiNo 谈判对话数据集，系统研究策略专属 Prefix Tuning 与共享 LoRA 在策略可控生成中的交互关系。实验发现，Prefix-only 在细粒度策略控制上最强，而 LoRA 能显著降低困惑度、改善生成适配；然而，简单 Prefix+LoRA 联合训练会降低策略一致性，形成明显的控制-质量折中。进一步的正交约束、策略分类辅助监督、梯度路由和对比生成损失实验表明，几何正交和内部表示可分性并不必然转化为生成文本中的策略一致性，token-level 生成损失也对策略差异不够敏感。此外，我们对 LLM-based 策略评估器进行校准，发现其在人类回复上的分类准确率有限，存在类别混淆和 shortcut 风险。本文的结果表明，混合 PEFT 在策略可控生成中并非自然协同，未来方法需要更直接地对齐生成行为与策略语义，而不仅仅依赖表示空间约束。

### 4.2 英文摘要草稿

Negotiation dialogue generation requires responses that are not only fluent and contextually appropriate, but also aligned with target negotiation strategies. Parameter-efficient fine-tuning (PEFT) provides a practical way to adapt large language models for controllable generation, yet the functional roles and interactions of different PEFT modules remain underexplored. In this paper, we conduct a diagnostic study of strategy-specific Prefix Tuning and shared LoRA adaptation for strategy-controlled negotiation dialogue generation on the CaSiNo dataset. Our experiments show that Prefix-only tuning provides the strongest fine-grained strategy controllability, while LoRA substantially improves perplexity and language adaptation. However, naively combining Prefix and LoRA degrades strategy consistency, revealing a clear control-quality trade-off. Further diagnostic experiments with orthogonal regularization, auxiliary strategy classification, gradient routing, and contrastive generation objectives show that neither geometric separation nor internal strategy separability reliably translates into strategy-consistent generations. We also calibrate the LLM-based strategy evaluator on human-written responses and identify substantial class ambiguity and shortcut risks. These findings suggest that hybrid PEFT modules do not automatically cooperate in strategy-controlled generation, and future methods should more directly align generation behavior with strategy semantics.

---

## 5. 论文结构建议

```text
Abstract
1. Introduction
2. Related Work
3. Task and Diagnostic Framework
4. Experimental Setup
5. Main Results
6. Diagnostic Analysis
7. Evaluator Calibration and Reliability
8. Discussion
9. Conclusion
```

如果会议篇幅较短，可以合并为：

```text
1. Introduction
2. Related Work
3. Method
4. Experiments
5. Analysis and Discussion
6. Conclusion
```

---

## 6. 各章节详细写法

### 6.1 Introduction

目标：把论文从“方法提升”转成“诊断分析”的合理性讲清楚。

建议结构：

1. 谈判对话需要策略可控生成。
   - 不是只要流畅，还要能体现 elicit-pref、self-need、vouch-fair 等策略。
2. 大语言模型可以生成自然回复，但直接文本指令控制不稳定。
3. PEFT 是可行方向：Prefix 可以注入控制信号，LoRA 可以做领域适配。
4. 但问题是：Prefix 和 LoRA 是否能自然协同？
5. 直觉假设：Prefix 负责策略，LoRA 负责生成质量。
6. 本文发现：这个假设并不自动成立。LoRA 降低 PPL，但会削弱 Prefix 的策略控制。
7. 本文贡献：系统诊断、trade-off、缓解机制局限、评估器校准。

可用句子：

> A natural hypothesis is that strategy-specific Prefix can serve as a control interface while LoRA handles domain adaptation. Our experiments show that this division of labor does not emerge automatically.

---

### 6.2 Related Work

建议分四类：

1. **Negotiation Dialogue Generation**
   - CaSiNo 数据集；
   - 谈判对话中的策略、偏好、需求表达；
   - 以前方法可能关注 outcome、persuasion 或 dialogue act。

2. **Controllable Text Generation**
   - 通过 prompt、control code、prefix、adapter 控制生成；
   - 策略控制与情感/话题控制不同，因为策略边界更模糊。

3. **Parameter-Efficient Fine-Tuning**
   - Prefix Tuning；
   - LoRA；
   - Adapter / Prompt Tuning；
   - Hybrid PEFT。

4. **LLM-as-judge Evaluation**
   - LLM 评估生成质量与语义属性；
   - 评估器偏见与校准问题；
   - 本文贡献是对策略分类 judge 做校准。

注意：Related Work 不要写太长，重点服务本文主线。

---

### 6.3 Task and Diagnostic Framework / Method

需要写清楚任务和模型组件。

#### 任务定义

输入：
- dialogue context \(x\)
- target strategy \(s \in S\)，共有 9 类策略

输出：
- 下一轮 utterance \(y\)

目标：
- 生成回复既要符合上下文，又要体现目标策略。

#### 模型组件

1. **Strategy-specific Prefix Bank**
   - 每个策略一个 prefix embedding；
   - 20 virtual tokens；
   - 作为策略控制信号。

2. **Shared LoRA Branch**
   - 作用于 q_proj/k_proj/v_proj/o_proj；
   - 共享于所有策略；
   - 目标是语言建模和领域适配。

3. **Hybrid Prefix-LoRA**
   - 同时启用 Prefix 和 LoRA；
   - 检验是否自然分工。

4. **Orthogonal Regularization**
   - 比较 Prefix delta 与 LoRA delta；
   - 局部 token-level + 全局 sequence-level cosine。

5. **Auxiliary Strategy Classification**
   - 在 hidden/delta_prefix 上做策略分类；
   - 试图增强 Prefix 的策略表征。

6. **Gradient Routing**
   - gen_loss 不更新 Prefix；
   - cls_loss 更新 Prefix；
   - 用于诊断梯度冲突。

7. **Contrastive Generation Loss**
   - 正确策略 Prefix 的 gen_loss 应小于错误策略 Prefix；
   - 实验发现实际 loss 几乎为 0。

不要把所有组件都包装成最终方法；应写成 diagnostic variants。

---

### 6.4 Experimental Setup

应包含：

- Dataset: CaSiNo + augmented training set。
- 策略类别：9 类。
- 数据过滤：排除 non-strategic，多标签样本 drop。
- 训练数据：增强后 610 dialogues / 5285 单标签 turn。
- Valid/test：原始验证/测试，不增强。
- Base model: Qwen3-8B, 4-bit, bfloat16。
- Prefix: 20 virtual tokens。
- LoRA: r=16, alpha=32, dropout=0.05, target q/k/v/o。
- 训练：batch size 1, AdamW, LoRA lr 1e-4, Prefix lr 5e-4。
- 评估：PPL + swap strategy evaluation。
- Swap evaluation: 30 contexts × 9 strategies = 270 generations。
- LLM judge: Qwen3-8B as classifier。

必须写的限制：

> Since LLM-based strategy evaluation is imperfect, we additionally calibrate the evaluator on human-written validation responses and report class-level behavior.

---

## 7. 结果表设计

### Table 1：Main Comparison

建议放主文最重要位置。

| Method | Strategy input | Trainable PEFT | Prefix | LoRA | Valid PPL | Strategy Acc | Role |
|---|---|---|---|---|---:|---:|---|
| Frozen Base + Strategy Text | Text | None | ✗ | ✗ | — | 27.41 | instruction baseline |
| LoRA-only old | None | LoRA | ✗ | ✓ | 8.03 | 11.11 | diagnostic only |
| LoRA + Strategy Text | Text | LoRA | ✗ | ✓ | 7.81 | 42.22 | fair LoRA text baseline |
| Prefix-only | Strategy ID | Prefix | ✓ | ✗ | 10.40/10.56 | 55.93/48.69 | strongest control |
| Prefix+LoRA | Strategy ID | Prefix+LoRA | ✓ | ✓ | 7.76/8.14 | 37.17/41.85 | quality-control trade-off |
| Warm-start | Prefix init | Prefix+LoRA | ✓ | ✓ | 8.03/8.02 | 41.85/45.56 | promising trade-off |
| Prefix→LoRA | frozen Prefix | LoRA only | ✓ frozen | ✓ | 8.01/8.06 | 38.89/44.44 | staged trade-off |

注意：最终论文中不要混用太多不同实验 tag 的数字。建议：
- 主表优先用 `casino_augmented_new_fix`，因为它包含公平 B2_text_lora；
- 诊断表用 `casino_augmented_fix_b6`，因为它包含 B6 系列；
- 若混用，表注必须说明不同实验组和目的。

---

### Table 2：Diagnostic Variants

| Variant | Purpose | Valid PPL | Strategy Acc | Finding |
|---|---|---:|---:|---|
| B5 Orth | geometric separation | 7.80 | 36.06 | weak orth hurts control |
| B6_fix | orth + cls after bug fix | 7.97 | 31.85 | stable but poor control |
| B6_cls_5.0 | stronger cls | 7.92 | 39.63 | internal classification not enough |
| B6_orth_1.0 | stronger orth | 8.14 | 40.37 | geometric orthogonality not functional disentanglement |
| B6_grad | gradient routing | 7.91 | 37.78 | gradient conflict partial but insufficient |
| B6_contrastive | contrastive generation | 8.06 | 42.59 | contrastive loss not triggered |

---

### Table 3：Evaluator Calibration

| Strategy | Human response calibration acc | Model behavior note |
|---|---:|---|
| elicit-pref | 84.2 | relatively reliable |
| small-talk | 57.7 | often confused with empathy |
| promote-coordination | 51.7 | confused with self-need |
| self-need | 51.4 | evaluator has bias toward this class |
| showing-empathy | 40.0 | moderate reliability |
| vouch-fair | 35.7 | difficult |
| other-need | 30.0 | unreliable |
| uv-part | 25.0 | unreliable |
| no-need | 0.0 | shortcut risk |

---

### Table 4：Multi-seed Results（待补）

| Method | Seed 42 Acc | Seed 43 Acc | Seed 44 Acc | Mean ± Std | Valid PPL Mean | Conclusion |
|---|---:|---:|---:|---:|---:|---|
| Prefix-only | pending | pending | pending | pending | around 10.5 | control baseline |
| Prefix+LoRA | pending | pending | pending | pending | around 7.9 | PPL stable |
| Warm-start | pending | pending | pending | pending | around 8.0 | trade-off? |
| Prefix→LoRA | pending | pending | pending | pending | around 8.0 | trade-off? |

当前只能写：PPL 趋势已稳定，策略准确率等待评估。

---

## 8. 推荐图设计

### Figure 1：方法框架图

展示：
- 输入 dialogue context；
- strategy ID 选择对应 Prefix；
- LoRA 作用于 attention projection；
- 输出 response；
- 旁边标出 orth / cls / grad routing / contrastive 是 diagnostic variants。

### Figure 2：Control-Quality Trade-off 散点图

横轴：Valid PPL 或 Test PPL。  
纵轴：Strategy Accuracy。  
点：B1/B2_text_lora/B3/B4/B5/B6 variants/B7/B9。

预期视觉结论：
- B3 在高 accuracy 高 PPL 区域；
- B4/B6 在低 PPL 中等 accuracy 区域；
- B7/B9 在中间折中区域；
- B1 在低 accuracy；
- B2_text_lora 是公平 LoRA baseline。

### Figure 3：Per-class Accuracy / Confusion

展示 B3 vs B4 vs B9 或 B2_text_lora vs B3。

重点看：
- no-need shortcut；
- other-need / uv-part 难；
- LoRA 加入后哪些策略掉得最明显。

### Figure 4（可选）：Evaluator Calibration

展示人工文本上的每类校准准确率，说明 LLM judge 不是完美标签。

---

## 9. 哪些结论可以写强，哪些必须写弱

### 9.1 可以写强的结论

1. **Prefix-only 在当前设置下是最强策略控制基线。**
   - 支持：旧 B3 48.69%，新 B3 55.93%。

2. **LoRA 稳定降低 PPL。**
   - 支持：B4/B7/B9 的 valid PPL 稳定在约 7.8–8.1，而 B3 约 10.4–10.6。

3. **Prefix+LoRA 存在 control-quality trade-off。**
   - 支持：PPL 降低但 strategy acc 下降。

4. **简单 orth/cls/grad/contrastive 不能稳定恢复 Prefix-only 控制能力。**
   - 支持：B6 系列均未超过 B3。

5. **LLM judge 需要校准。**
   - 支持：人工文本校准准确率 52%，类别差异大。

---

### 9.2 必须弱化的结论

1. 不要写：DeST-RS significantly outperforms all baselines。  
   应写：DeST-RS-style hybrid variants are useful diagnostic tools, but do not consistently outperform Prefix-only.

2. 不要写：B3 is the theoretical upper bound。  
   应写：B3 is the strongest single-branch strategy-control baseline under our current setting.

3. 不要写：52% is the theoretical ceiling。  
   应写：The evaluator obtains 52% accuracy on human-written responses, indicating substantial ambiguity and evaluator limitations.

4. 不要写：LoRA-induced environment shift is the only reason。  
   应写：The results are consistent with the hypothesis that LoRA changes the operating environment of Prefix, but further representation analysis is needed for direct evidence.

5. 不要写：B7/B9 is the final best method。  
   应写：Warm-start and staged training are promising trade-off strategies, pending multi-seed validation.

---

## 10. 论文中可用的核心论证链

建议按以下逻辑写 Results/Analysis：

```text
Step 1: 文本指令有一定控制能力，但不足。
  B1 = 27.41%。

Step 2: 公平 LoRA 文本基线可以控制策略，但不如 Prefix。
  B2_text_lora = 42.22%，B3 = 55.93%。

Step 3: Prefix-only 策略控制最强，但 PPL 高。
  B3 PPL ≈ 10.4–10.6。

Step 4: 加 LoRA 后 PPL 大幅下降，但策略控制下降。
  B4 PPL ≈ 7.8–8.1，Acc 下降。

Step 5: orth/cls 没有解决。
  B5/B6_fix/B6_cls/B6_orth 均低于 B3。

Step 6: gradient routing 说明梯度冲突只是部分原因。
  B6_grad 比 B6_fix 好，但仍低。

Step 7: contrastive loss 为 0，说明 token-level generation loss 不敏感于策略差异。

Step 8: B7/B9 提供可能折中，但不能写成稳定最优。

Step 9: LLM judge 校准说明自动准确率需要谨慎解释。
```

---

## 11. 需要继续补的实验与写作阻塞项

### 11.1 最高优先级

1. **跑完多 seed 的 strategy_eval_llm**
   - seed 42/43/44；
   - B3/B4/B7/B9；
   - 如果有算力，也包括 B2_text_lora。

2. **计算 bootstrap confidence interval**
   - 对 270 条 swap samples 先做；
   - 最好扩到 100 contexts × 9 = 900 generations。

3. **补人工评估小样本**
   - 每模型 45 条，B1/B2_text_lora/B3/B4/B7/B9；
   - 标注 target strategy 是否体现、fluency、context relevance、shortcut。

### 11.2 中等优先级

4. **LoRA on/off Prefix effect 表示分析**
   - 同一输入下比较 Base+Prefix 与 LoRA+Prefix；
   - 指标：prefix-induced hidden delta cosine、norm ratio、layer-wise change。
   - 目的：给“LoRA 改变 Prefix 运行环境”提供直接证据。

5. **per-class breakdown 和 confusion matrix**
   - 特别关注 no-need、other-need、uv-part、self-need。

### 11.3 如果时间有限，可以不做但需在 limitation 写明

- 更大模型；
- 更多数据集；
- 人类双标注一致性；
- 更复杂的 control reward 或 RL 方法。

---

## 12. Limitations 写法

建议主动写 limitations，避免审稿人认为我们夸大结论。

可以写：

1. **Single dataset**：目前只在 CaSiNo 上验证。
2. **Evaluator reliability**：LLM judge 有类别混淆，虽然做了校准，但仍不能替代大规模人工评估。
3. **Limited seeds / sample size**：多 seed 和更大 swap evaluation 仍需补充。
4. **Mechanism evidence**：LoRA-induced environment shift 目前主要由诊断实验间接支持，仍需更直接的表示空间分析。
5. **Model scope**：实验基于 Qwen3-8B，其他模型可能表现不同。

英文示例：

> Our analysis of LoRA-induced Prefix degradation is primarily supported by diagnostic interventions rather than direct causal representation analysis. Future work should examine layer-wise attention and hidden-state changes under LoRA on/off conditions.

---

## 13. 给网页端 AI 的总提示词

下面这段可以直接复制给任意网页端 AI，让它基于本文档和 `实验完整记录.md` 帮你写论文。

```text
我正在写一篇关于谈判对话策略可控生成的论文。请注意，论文定位不是“提出一个显著优于所有 baseline 的新方法”，而是“Prefix-LoRA 混合 PEFT 的诊断分析与控制-质量折中研究”。

核心实验基于 CaSiNo 数据集和 Qwen3-8B，比较 Frozen Base + strategy text、LoRA + strategy text、Prefix-only、Prefix+LoRA、Orth、Cls、Gradient Routing、Contrastive Loss、Warm-start 和 Prefix→LoRA 等变体。

请围绕以下主线写作：
1. Prefix-only 在当前设置下提供最强细粒度策略控制；
2. LoRA 显著降低 PPL、改善生成适配；
3. Prefix+LoRA 联合训练带来 control-quality trade-off：PPL 更好但策略准确率下降；
4. Orth、Cls、Gradient Routing、Contrastive Loss 都不能稳定恢复 Prefix-only 的策略控制；
5. Warm-start 和 Prefix→LoRA 是有潜力的折中方案，但不能写成稳定最优；
6. LLM-as-judge 评估器在人类回复上校准准确率只有约 52%，说明自动策略准确率需要谨慎解释。

写作时请避免以下过强表述：
- 不要说 DeST-RS 显著优于所有 baseline；
- 不要说 B3 是理论 upper bound；
- 不要说 52% 是理论天花板；
- 不要说 LoRA-induced environment shift 是唯一原因；
- 不要说 B7/B9 是最终最优方法。

请用学术论文风格，帮我生成 [Introduction / Related Work / Method / Experiments / Analysis / Conclusion] 的初稿。
```

---

## 14. 给网页端 AI 的分章节提示词

### 14.1 写 Introduction

```text
请根据以下主线写论文 Introduction：
- 谈判对话生成需要同时满足流畅性、上下文相关性和策略一致性；
- 大语言模型可以生成自然回复，但直接文本指令的策略控制不稳定；
- PEFT 是一种高效适配方式，其中 Prefix 可能适合注入策略控制，LoRA 可能适合领域生成适配；
- 然而 Prefix 与 LoRA 是否能自然协同尚不清楚；
- 本文通过系统诊断实验发现：Prefix-only 策略控制强，LoRA 降低 PPL，但二者联合训练会产生 control-quality trade-off；
- orth/cls/grad routing/contrastive 等机制不能完全解决干扰；
- 本文还校准了 LLM-as-judge 策略评估器，发现评估可靠性问题。
请写成英文会议论文风格，最后列出 3-4 条 contributions。
```

### 14.2 写 Method

```text
请写 Method 部分，介绍一个用于诊断 Prefix-LoRA 混合 PEFT 的框架，而不是夸大成最终 SOTA 方法。需要包括：
1. Task formulation: input dialogue context x, target strategy s, output response y；
2. Strategy-specific Prefix Bank: 每个策略一个 prefix，20 virtual tokens；
3. Shared LoRA: 作用于 q/k/v/o projection，负责领域生成适配；
4. Joint Prefix-LoRA；
5. Orthogonal regularization: Prefix delta 与 LoRA delta 的 token-level 和 sequence-level cosine；
6. Auxiliary strategy classification；
7. Diagnostic variants: gradient routing and contrastive generation loss。
请保持学术、克制，不要声称所有组件都会提升性能。
```

### 14.3 写 Experiments

```text
请写 Experiments 部分，基于以下设置：
- Dataset: CaSiNo + augmented training set, 9 strategies；
- Base model: Qwen3-8B, 4-bit, bfloat16；
- Prefix: 20 virtual tokens；
- LoRA: r=16, alpha=32, dropout=0.05, target q/k/v/o；
- Evaluation: perplexity and swap strategy evaluation；
- Swap evaluation: 30 contexts × 9 strategies = 270 generations；
- LLM judge: Qwen3-8B strategy classifier；
- Evaluator calibration on human-written validation responses shows 52.0% accuracy。
需要组织 baseline：Frozen Base + strategy text, LoRA + strategy text, Prefix-only, Prefix+LoRA, Orth/Cls variants, Gradient Routing, Contrastive, Warm-start, Prefix→LoRA。
```

### 14.4 写 Results and Analysis

```text
请写 Results and Analysis 部分，围绕以下发现：
1. Prefix-only achieves the strongest strategy controllability but higher PPL；
2. LoRA + strategy text is a stronger and fairer LoRA baseline than old LoRA-only, but remains below Prefix-only；
3. Prefix+LoRA lowers PPL but degrades strategy accuracy, revealing control-quality trade-off；
4. Orthogonal regularization and auxiliary classification do not reliably recover strategy control；
5. Gradient routing improves over B6_fix but remains insufficient, suggesting gradient conflict is only part of the issue；
6. Contrastive generation loss is nearly zero, indicating token-level LM loss is not sensitive to strategy changes；
7. Warm-start and Prefix→LoRA are promising trade-offs but need multi-seed validation；
8. LLM judge calibration reveals evaluator ambiguity and shortcut risk。
请避免过强 claim。
```

### 14.5 写 Limitations

```text
请写 Limitations 部分，主动承认：
1. 当前实验主要在 CaSiNo 和 Qwen3-8B 上；
2. LLM-as-judge 策略评估有可靠性问题，虽然做了校准；
3. 需要更多 seed、更大 swap evaluation 和人工评估；
4. LoRA-induced environment shift 目前主要由诊断实验间接支持，还需要 layer-wise representation analysis；
5. B7/B9 是 promising trade-off，不是稳定最优。
请写得诚实但不削弱论文价值。
```

---

## 15. 当前写作状态建议

可以立即开始写：
- Introduction；
- Related Work；
- Task/Method；
- Experimental Setup；
- 初版 Results；
- Discussion/Limitations。

暂缓定稿：
- 最终主结果表；
- 多 seed 表；
- 置信区间；
- B7/B9 是否作为贡献点；
- “LoRA-induced environment shift” 的强度表述。

最终判断：

> 现在已经可以开始论文初稿。实验还没完全跑完不会阻塞写作主体，但会影响最终结果表、显著性分析和结论强弱。
