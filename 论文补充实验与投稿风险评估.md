# 论文补充实验与投稿风险评估

> 日期：2026-06-23  
> 目的：记录对当前代码与《实验完整记录.md》的审查意见，用于后续补实验、改论文叙事和判断 CCF-C 投稿风险。

---

## 1. 总体判断

当前项目已经具备论文雏形，尤其适合写成**诊断分析型论文**：系统研究 Prefix Tuning 与 LoRA 在谈判策略可控生成中的功能分工、干扰来源和控制-生成质量 trade-off。实验链条较完整，代码结构也基本能支撑现有实验结论。

但当前版本还不建议直接按“提出 DeST-RS 并显著提升性能”的方式投稿 CCF-C。原因是：完整 DeST-RS（orth + cls）并未超过 Prefix-only，也没有稳定超过简单 Prefix+LoRA；最强策略控制仍是 B3 Prefix-only。因此论文叙事应从“方法提升型”调整为“混合 PEFT 诊断分析 + 实用折中方案”。

**建议定位**：

- 不写成：我们提出一个新方法，显著提升策略控制。
- 改写成：我们系统诊断混合 PEFT 在策略可控生成中的干扰机制，发现 Prefix 更适合策略 steering，LoRA 更适合语言适配，但二者联合存在控制-质量 trade-off；两阶段 Prefix→LoRA 训练提供较好的折中。

---

## 2. 当前实验能较稳支撑的结论

### 2.1 Prefix 比 LoRA 更适合策略控制

证据：

- B2 LoRA-only：11.11%，接近随机。
- B3 Prefix-only：48.69%，显著高于 B2 和 B1。
- B1 Frozen Base + 文本指令：27.41%，说明文本指令有一定控制能力，但弱于 Prefix。

这个结论相对稳，可以作为论文核心发现之一。

### 2.2 Prefix 与 LoRA 联合训练存在控制-质量 trade-off

证据：

- B3：策略准确率最高，但 PPL 较高。
- B4：PPL 明显下降，但策略准确率从 48.69% 降到 41.85%。
- B7/B9：能在策略控制和 PPL 之间取得较好折中，但没有超过 B3 的策略准确率。

这个结论也比较稳，适合作为论文主线。

### 2.3 简单正交约束、分类辅助监督、梯度路由、对比生成损失都不能彻底解决干扰

证据：

- B5/B6_fix/B6_cls/B6_orth 均未超过 B4 或 B3。
- B6_grad 相比 B6_fix 有提升，但仍低于 B4/B3，说明梯度冲突只是部分原因。
- B6_contrastive 中 contrastive_loss 基本为 0，说明普通 token-level gen_loss 对策略差异不敏感。

这个结论适合写成“诊断实验发现”，而不是“方法失败”。

### 2.4 LLM-as-judge 策略评估存在明显可靠性瓶颈

证据：

- 评估器在人类验证集回复上的准确率只有 52.0%。
- no-need、other-need、uv-part 等类别尤其不可靠。
- no-need 在模型生成上高准确率但在人类文本上为 0%，提示 shortcut learning。

这个是论文亮点之一，但解释时需要谨慎，不能直接称为“理论天花板”。

---

## 3. 当前不宜写得过强的结论

### 3.1 不要把 52.0% 直接称为“理论天花板”

当前《实验完整记录.md》中有“B3 达到评估天花板 93.6%”的表述，但严格来说，评估器对人工文本的准确率不等于它对模型生成文本的理论上限。模型可能生成更显式、更模板化的策略表达，反而更容易被 LLM judge 判对。

建议改写为：

> 评估器在人类回复上的准确率只有 52.0%，说明该任务存在显著语义模糊和评估器可靠性问题。因此模型策略准确率应结合评估器校准、类别混淆和人工评估共同解释，而不能被视为绝对真实性能。

### 3.2 不要写“环境级不兼容是唯一原因”

B9 冻结 B3 Prefix 后仍下降，确实支持 LoRA 改变 Prefix 工作环境的解释；但目前缺少直接表示空间证据、多 seed 统计和显著性检验，因此“唯一原因”太强。

建议改写为：

> 当前实验最一致地支持的解释是：LoRA 对 Q/K/V/O 投影的改变可能削弱了 Prefix 在原始模型环境中学到的策略 steering 信号。这种环境变化是策略控制退化的重要原因之一，但仍需表示分析和多 seed 实验进一步验证。

### 3.3 不要把 B3 称为严格 upper bound

B3 可以称为“当前实验设置下的最强单分支策略控制基线”，但不应称为理论上界或方法上界。

建议改写为：

> B3 Prefix-only 是当前实验设置下的最强单分支策略控制基线。

---

## 4. CCF-C 投稿风险点

当前如果直接投稿 CCF-C，主要风险如下：

| 风险点 | 当前状态 | 可能被审稿人质疑 |
|---|---|---|
| 策略评估样本少 | 30 contexts × 9 = 270 条 | 差异是否显著？是否偶然？ |
| 无多随机种子 | 多数实验单 seed | 结果稳定性不足 |
| 无置信区间/显著性检验 | 只报单点 accuracy | B7/B9/B4 差距可能只有几条样本 |
| LLM judge 准确率低 | 校准只有 52% | 评估指标是否可信？ |
| 缺少人工评估 | 目前主要依赖 LLM judge | 生成质量和策略真实性不足 |
| LoRA baseline 不够公平 | B2 不接收策略 ID | LoRA-only 接近随机可能是 strawman |
| “环境级不兼容”缺直接证据 | 主要来自 B9 间接推理 | 需要 hidden/attention 表示分析 |
| 方法结果不是全面最优 | B3 策略准确率最高 | 不能写成新方法显著提升 |

---

## 5. 最优先补充的实验

### 5.1 必补 1：扩大 swap evaluation

当前策略评估只有 270 条，建议至少扩展到：

```text
100 contexts × 9 strategies = 900 generations
```

优先对以下模型补：

- B1 Frozen Base + instruction
- B2 LoRA-only
- B3 Prefix-only
- B4 Prefix+LoRA
- B7 warm-start
- B9 Prefix→LoRA

报告指标建议包括：

- overall accuracy
- macro-F1
- per-class accuracy
- bootstrap 95% confidence interval

目的：判断 B3 vs B4、B4 vs B7/B9 的差异是否真正稳定，而不是 270 条样本中的偶然波动。

### 5.2 必补 2：多 seed 实验

至少补 3 个 seed：

```text
seed = 42, 43, 44
```

如果算力有限，优先补：

- B3 Prefix-only
- B4 Prefix+LoRA
- B7 warm-start
- B9 Prefix→LoRA

目标是判断核心结论的稳定性：

- Prefix-only 是否总是策略控制最强；
- Prefix+LoRA 是否稳定产生策略下降；
- B7/B9 是否稳定优于 B4；
- B7/B9 与 B4 的差异是否显著。

### 5.3 必补 3：人工评估小样本

由于 LLM judge 校准准确率只有 52%，建议加入人工评估作为 sanity check。

建议抽样：

```text
每个模型 45 条：5 contexts × 9 strategies
模型：B1/B3/B4/B7/B9
总计约 225 条
```

人工标注维度：

1. 是否体现目标策略；
2. fluency 1–5；
3. context relevance 1–5；
4. 是否存在模板化/shortcut 表达。

作用：

- 缓解“只依赖 LLM judge”的审稿风险；
- 验证 no-need、self-need 等类别是否存在 shortcut；
- 给 PPL 之外的生成质量提供人类判断。

### 5.4 必补 4：公平的 LoRA + strategy text baseline

当前 B2 LoRA-only 不接收策略 ID，因此 11.11% 可能被认为是不公平 baseline。建议新增一个 baseline：

```text
Prompt 中显式加入策略名称或策略说明，然后只训练 LoRA。
```

例如：

```text
Use the following negotiation strategy: {strategy}.
Generate the target speaker's next utterance.
```

这个实验很关键：

- 如果该 baseline 仍明显弱于 Prefix，说明 Prefix 的策略 steering 价值更稳。
- 如果该 baseline 接近或超过 Prefix，需要重新调整论文叙事。

建议命名：

```text
B2_text_lora 或 B1_lora_instruction
```

报告时与 B1、B2、B3 对比：

| 方法 | 是否有策略文本 | 是否训练 LoRA | 是否训练 Prefix | 作用 |
|---|---|---|---|---|
| B1 | 是 | 否 | 否 | 纯文本指令基线 |
| B2 | 否 | 是 | 否 | LoRA 无策略输入 |
| B2_text_lora | 是 | 是 | 否 | 公平 LoRA 策略文本基线 |
| B3 | 隐式策略 ID via Prefix | 否 | 是 | Prefix 策略控制基线 |

### 5.5 必补 5：表示空间/注意力环境分析

为支撑“LoRA 改变 Prefix 工作环境”的解释，建议补一个直接分析。

对同一批输入比较：

1. Base + Prefix；
2. LoRA + 同一个冻结 Prefix。

可计算：

- prefix-induced hidden delta 的 cosine similarity；
- LoRA on/off 后 prefix effect norm 的变化；
- 不同层 hidden representation 的 CKA/cosine；
- attention output 或 Q/K/V/O 投影前后差异；
- 按策略分组分析 other-need、uv-part 为什么被 LoRA 抹掉。

这类分析能把 B9 的间接推理变成更直接的机制证据。

建议先做一个轻量版本：

```text
输入：同一批 valid contexts，每个 strategy prefix 跑一次。
比较：LoRA off vs LoRA on 时，prefix 导致的 hidden delta。
指标：cos(delta_prefix_base, delta_prefix_lora)、norm ratio、per-layer change。
输出：整体均值 + 各策略均值。
```

如果发现 other-need、uv-part 的 prefix effect 在 LoRA on 后明显减弱，就能很好支撑当前观察。

---

## 6. 推荐论文叙事

### 6.1 推荐标题方向

1. *When Prefix Meets LoRA: Diagnosing Hybrid PEFT Interference in Strategy-Controlled Negotiation Dialogue Generation*
2. *A Diagnostic Study of Prefix-LoRA Adaptation for Strategy-Controlled Negotiation Dialogue*
3. *Understanding Control-Quality Trade-offs in Hybrid PEFT for Negotiation Strategy Generation*

### 6.2 推荐摘要逻辑

```text
问题：谈判策略可控生成需要同时满足策略控制和语言质量。
观察：Prefix tuning 擅长策略 steering，LoRA 擅长生成适配，但二者联合会产生干扰。
方法：通过 LoRA-only、Prefix-only、Prefix+LoRA、orth、cls、gradient routing、contrastive、frozen-prefix 等诊断实验系统排查干扰来源。
发现：干扰不能仅由梯度冲突或损失设计解释，LoRA 改变 Prefix 的表示运行环境可能是重要原因。
评估：校准 LLM-as-judge，发现策略评估存在显著类别混淆和 shortcut 风险。
结论：两阶段 Prefix→LoRA 训练提供策略控制与生成质量的较好折中。
```

### 6.3 推荐贡献写法

建议贡献写成：

1. **系统诊断混合 PEFT 在策略可控生成中的功能边界**：通过 B1-B9 及后续变体，比较文本指令、LoRA、Prefix、Prefix+LoRA、orth、cls、gradient routing、contrastive、two-stage training。
2. **揭示 Prefix-LoRA 联合训练中的控制-质量 trade-off 与干扰现象**：Prefix-only 策略控制强但 PPL 高；LoRA 改善 PPL 但削弱策略控制。
3. **校准 LLM-based 策略评估器**：发现 LLM judge 在人类回复上的准确率仅 52%，并揭示类别混淆和 shortcut learning。
4. **验证两阶段 Prefix→LoRA 训练作为实用折中方案**：在保持较高策略控制的同时显著降低 PPL。

---

## 7. 建议的论文结构

```text
Abstract
├── 问题：策略可控生成需要兼顾控制能力与语言质量
├── 发现：Prefix 与 LoRA 的功能分工并不自动协同
├── 方法：系统诊断实验 + LLM judge 校准
├── 结论：Prefix 适合策略控制，LoRA 适合生成适配，二者联合存在干扰；两阶段训练是较好折中

1. Introduction
   - 谈判策略可控生成的重要性
   - PEFT 在大模型适配中的作用
   - Prefix 与 LoRA 是否能自然分工？
   - 本文贡献

2. Related Work
   - Negotiation dialogue generation
   - Strategy-controlled generation
   - Parameter-efficient fine-tuning
   - LLM-as-judge evaluation

3. Method and Diagnostic Setup
   - Task formulation
   - Strategy-specific Prefix Bank
   - Shared LoRA branch
   - Orthogonal constraint
   - Strategy classification auxiliary loss
   - Gradient routing and contrastive variants

4. Experiments
   4.1 Dataset and preprocessing
   4.2 Evaluation protocol
   4.3 Main baselines: B1/B2/B3/B4
   4.4 Orthogonal and classification variants: B5/B6
   4.5 Gradient routing and contrastive diagnostics
   4.6 Two-stage training: B7/B9
   4.7 LLM judge calibration
   4.8 Human evaluation / extended evaluation（补实验后加入）

5. Analysis and Discussion
   - Control-quality trade-off
   - Why cls/orth/contrastive do not fully work
   - LoRA-induced prefix environment shift（补表示分析后加入）
   - Evaluator reliability and shortcut learning

6. Conclusion
```

---

## 8. 当前可投性结论

当前版本：

- 作为课程论文、硕士论文实验章节、内部技术报告：基本足够；
- 作为 workshop/短论文：有机会，但要弱化结论；
- 作为 CCF-C 正式论文：还需补实验，尤其是扩大评估、多 seed、公平 LoRA baseline、人工评估和表示分析。

建议把当前准备度视为 **60%–70%**。

如果能补完以下最小集合，再投稿 CCF-C 会更稳：

1. 扩大 swap evaluation；
2. B3/B4/B7/B9 至少 3 seed；
3. 加 macro-F1 和 bootstrap confidence interval；
4. 加小规模人工评估；
5. 加公平 LoRA + strategy text baseline；
6. 加 LoRA on/off 对 Prefix hidden effect 的表示分析。

---

## 9. 晚上优先检查清单

如果时间有限，建议按下面顺序看：

1. **先检查 B2 是否需要补公平 baseline**：这是最容易被审稿人质疑的点。
2. **看现有代码能否快速把 demo_num_examples 从 30 改到 100 或全量 valid/test**。
3. **确认哪些实验权重还在，能否直接重新生成更多 swap samples，而不必重训**。
4. **确认 B3/B4/B7/B9 是否能用不同 seed 快速重训**。
5. **看 `scripts/analyze_orthogonality.py` 能否改成 LoRA on/off 的 Prefix effect 分析脚本**。
6. **准备人工评估表格模板**，先抽 200 条左右做人工 sanity check。

---

## 10. 一句话结论

当前项目不是不能写论文，而是应该从“我提出的方法提升了性能”改成“我系统诊断了混合 PEFT 在策略可控生成中的干扰机制，并提出了实用折中训练方案”。按这个方向补实验和降调结论后，才更接近 CCF-C 可投标准。
