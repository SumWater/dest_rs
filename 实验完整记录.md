# DeST-RS 实验完整记录

> 最后更新：2026-06-22

---

## 一、项目背景

**研究方向**：谈判对话中的策略可控生成。基于 CaSiNo 数据集 + Qwen3-8B 基座模型，研究混合 PEFT（Prefix Tuning + LoRA）在策略控制与生成适配中的功能分工。

**技术组件**：
- Strategy-specific Prefix：9 个策略各对应一个可训练的 prefix embedding（20 virtual tokens）
- Shared LoRA：挂载在 Q/K/V/O 投影上的低秩适配矩阵（r=16, α=32）
- 局部-全局正交约束：约束 Prefix delta 和 LoRA delta 在 token 级和序列级的 cosine similarity
- 策略分类辅助监督：在中间层 hidden state 上做 9 类策略分类
- 梯度路由：gen_loss 梯度只流向 LoRA，cls_loss 梯度只流向 Prefix

**目标投稿**：ICIG 2026（CCF-C 会议），截止 2026-06-30。

---

## 二、数据与评估

### 2.1 数据集

| 属性 | 原始 CaSiNo | 增强版本（实际使用） |
|---|---|---|
| 训练 dialogue 数 | 314 | 610 |
| 训练可用单标签 turn | 1733 | 5285 |
| 策略分布 | 严重不均（15:1）| 基本均衡（2:1）|
| 验证 dialogue 数 | 41 | 41（未增强）|
| 验证可用单标签 turn | 230 | 230 |
| 测试 dialogue 数 | 41 | 41（未增强）|

### 2.2 数据预处理问题

**53% 的原始标注被丢弃**：
- 31% 是 "non-strategic"（被 exclude_labels 过滤）
- 22% 是多标签标注（如 "self-need,other-need,small-talk"，被 multi_label_policy="drop" 过滤）

**9 个策略的增强后训练分布**：
| 策略 | 训练样本数 |
|---|---|
| small-talk | 912 |
| promote-coordination | 766 |
| self-need | 722 |
| vouch-fair | 540 |
| elicit-pref | 501 |
| other-need | 466 |
| showing-empathy | 461 |
| no-need | 460 |
| uv-part | 457 |

### 2.3 评估方式

**Swap Samples 评估**：
- 从验证集取 30 个对话上下文
- 对每个上下文，用 9 个不同策略 prefix 各生成一次回复（共 270 条）
- 用 LLM（Qwen3-8B）作为策略评估器，判断每条生成文本体现的策略
- 指标：9 类策略准确率、4 类粗粒度准确率

**评估器校准（2026-06-22）**：
- 用人工标注的 ground truth 验证集回复测试 LLM 评估器
- 结果：评估器对人工文本的分类准确率仅为 **52.0%**
- 这意味着任务存在 48% 的不可消除噪声
- B3 的 48.7% 达到了评估天花板的 93.6%

---

## 三、实验系统总览

所有实验基于以下固定配置：
- 基座模型：Qwen3-8B（4-bit 量化，bfloat16）
- Prefix：20 virtual tokens，初始化标准差 0.02
- LoRA：r=16，α=32，dropout=0.05，target_modules=[q_proj, k_proj, v_proj, o_proj]
- 训练：batch_size=1，LoRA lr=1e-4，Prefix lr=5e-4，AdamW，grad_clip=1.0
- 评估：demo_num_examples=30，9 策略 × 30 上下文 = 270 条 swap 生成
- 数据集：augmented_data（增强后的训练集，610 dialogues，5285 单标签样本）

除特殊说明外，每个实验训练 2 个 epoch，保存验证集 PPL 最低的 checkpoint 做最终评估。

---

### 3.0 B1：Frozen Base + 文本指令（无 PEFT 基线）

**实验目的**：验证完全不使用 PEFT——直接用 Qwen3-8B 基座模型 + 文本策略指令——能否实现策略控制。作为所有 PEFT 实验的对比基线，回答"PEFT 到底有没有带来任何策略控制增益"。

**配置差异**：
```
不加载任何 PEFT 模块（无 Prefix、无 LoRA）
通过 prompt 文本注入策略指令：
  "As the next speaker, use the following negotiation strategy: {strategy}."
生成方式：逐 token greedy decoding（与 B2-B9 相同），max 40 tokens
后处理：strip <think> 标签（Qwen3 对指令会自发输出推理过程）
```

**策略评估**：9 类准确率 = **27.41%**。

**逐策略 breakdown**：
| 策略 | 准确率 |
|---|---|
| small-talk | 21/30 = 70.0% |
| elicit-pref | 18/30 = 60.0% |
| no-need | 12/30 = 40.0% |
| showing-empathy | 9/30 = 30.0% |
| vouch-fair | 6/30 = 20.0% |
| promote-coordination | 4/30 = 13.3% |
| self-need | 3/30 = 10.0% |
| other-need | 1/30 = 3.3% |
| uv-part | 0/30 = 0.0% |

**结论**：
- 27.41% 高于随机（11.1%），低于所有 PEFT 方法——文本指令能做策略控制，但效果有限
- Qwen3 在指令下仍然倾向输出 meta-instructions（"Do not use markdown..."），生成质量较差
- B1 确认了 PEFT（特别是 Prefix）在策略控制中的正向价值：B3 的 48.7% 比 B1 高出 21pp

---

### 3.1 B2：LoRA only（纯生成适配基线）

**实验目的**：验证只用 LoRA、不用 Prefix，模型能否学到策略控制。

**配置差异**：
```
adapter_mode = "lora_only"
enable_prefix = False     ← 完全不用 Prefix
enable_lora = True
train_lora = True
λ_orth = 0, λ_cls = 0
```

**训练过程**：
| epoch | gen_loss | valid PPL | test PPL |
|---|---|---|---|
| 1 | 1.47 | 8.03 | 8.33 |
| 2 | 1.21 | 8.33 | 8.57 |

**策略评估**：270 个 swap sample，9 类准确率 = 11.11%（接近随机猜测 1/9 ≈ 11.1%）。

**结论**：LoRA 只学了通用语言生成（PPL 8.03），完全没有学到策略条件控制。因为 LoRA 本身不接收策略 ID 输入——策略信息只能通过 Prefix 注入。

---

### 3.2 B3：Prefix only（纯策略控制基线，也是性能上界）

**实验目的**：验证只用 Prefix、不用 LoRA，模型能否实现策略控制。这是所有实验的 upper bound。

**配置差异**：
```
adapter_mode = "prefix_only"
enable_prefix = True
enable_lora = False       ← 完全不用 LoRA
train_prefix = True
λ_orth = 0, λ_cls = 0
```

**训练过程**：
| epoch | gen_loss | valid PPL | test PPL |
|---|---|---|---|
| 1 | 1.89 | 11.13 | 11.66 |
| 2 | 1.60 | 10.56 | 11.10 |

**策略评估**：9 类准确率 = **48.69%**（所有实验最高）。

**逐策略 breakdown**：
| 策略 | 准确率 | 评估器天花板 |
|---|---|---|
| elicit-pref | 60.0% | 84.2% |
| no-need | 96.7% | 0%（shortcut）|
| other-need | 26.7% | 30.0% |
| promote-coordination | 23.3% | 51.7% |
| self-need | 70.0% | 51.4% |
| showing-empathy | 70.0% | 40.0% |
| small-talk | 40.0% | 57.7% |
| uv-part | 25.9% | 25.0% |
| vouch-fair | 23.3% | 35.7% |

**关键观察**：
- no-need 的 96.7% 是虚假的——评估器无法识别人工写的 no-need（0%），但能识别模型生成的"夸张拒绝"模式。这是 shortcut learning。
- other-need（26.7%）和 uv-part（25.9%）已接近评估器天花板（30% 和 25%），再优化空间极小。
- Prefix 只用 gen_loss 训练，没有任何辅助监督，就能达到 48.7%。说明 gen_loss 本身可以驱动策略学习——不同策略对应不同的生成模式，Prefix 在降低 gen_loss 的过程中自然分化。

**PPL 代价**：10.56 是所有实验中 PPL 最高的。Prefix 学会了策略控制，但以牺牲生成流畅度为代价。

---

### 3.3 B4：Prefix + LoRA（联合训练，功能分工假说的第一次检验）

**实验目的**：验证 Prefix（策略控制）和 LoRA（生成适配）能否在联合训练中各自承担不同功能。

**配置差异**：
```
adapter_mode = "prefix_lora"
enable_prefix = True
enable_lora = True
train_prefix = True
train_lora = True
λ_orth = 0, λ_cls = 0
```

**训练过程**：
| epoch | gen_loss | valid PPL | test PPL |
|---|---|---|---|
| 1 | 1.46 | 8.31 | 8.61 |
| 2 | 1.19 | 8.14 | 8.39 |

**策略评估**：9 类准确率 = 41.85%。

**对比 B3**：
- PPL：8.14 vs 10.56（↓ 23%，生成质量显著改善）✅
- 策略准确率：41.85% vs 48.69%（↓ 7pp，策略控制退化）❌

**结论**：这是"功能分工假说"的第一次失败。加入 LoRA 后 PPL 下降（生成更好），但策略控制也下降了。如果 Prefix 和 LoRA 真的在分工——Prefix 管策略、LoRA 管生成——策略控制不应该受到 LoRA 的影响。但这 7pp 的降幅说明：**联合训练中存在某种干扰机制。**

此时我们还不知道干扰来自哪里。后续 B5-B9 的目标就是定位这个干扰的根因。

---

### 3.4 B5：Prefix + LoRA + 正交约束（首次尝试解决干扰）

**实验目的**：假设干扰来自 Prefix 和 LoRA 在 hidden space 中的表示耦合。加入局部-全局正交约束，让两者在几何上正交，看策略控制是否恢复。

**正交约束的计算**：
```python
delta_prefix = h(prefix_on, lora_off) - h(prefix_off, lora_off)
delta_lora   = h(prefix_off, lora_on)  - h(prefix_off, lora_off)

local_loss  = mean(cosine_similarity(delta_prefix[i], delta_lora[i])²)  # token 级
global_loss = mean(cosine_similarity(avg(delta_prefix), avg(delta_lora))²)  # 序列级
orth_loss   = 0.5 * local_loss + 0.5 * global_loss
```

**配置差异**：
```
adapter_mode = "prefix_lora_orth"
λ_orth = 0.05（权重很小）
orth_every_n_steps = 20（每 20 步计算一次正交）
orth_start_step = 50（前 50 步不计算，先让模型学会基础生成）
λ_cls = 0
```

**训练过程**：
| epoch | gen_loss | orth_loss | valid PPL | test PPL |
|---|---|---|---|---|
| 1 | 1.50 | 0.00185 | 7.80 | 8.00 |
| 2 | 1.19 | 0.00122 | 8.07 | 8.17 |

**策略评估**：9 类准确率 = 36.06%（比 B4 的 41.85% 更差）。

**问题分析**：
1. orth_loss 太弱：约 0.001-0.002，乘以 λ=0.05 后贡献约 0.00005-0.0001，相比 gen_loss（~1.2）完全被碾压
2. orth_every_n_steps=20 意味着绝大多数 step 没有正交约束，信号稀疏
3. 结果甚至不如 B4——正交约束不仅没帮助，还干扰了训练

**结论**：当前超参数下的正交约束无效。需要：（1）增大 λ_orth，（2）每步都算 orth，（3）从第 0 步开始。

---

### 3.5 B6：DeST-RS 完整版（含分类辅助监督，旧代码有 bug → 训练崩溃）

**实验目的**：在 B5 的基础上加入策略分类辅助监督（cls_loss），形成完整的 DeST-RS 方法。

**配置差异**：
```
adapter_mode = "dest_rs"
λ_orth = 0.05, λ_cls = 0.2
cls_target = "delta_prefix"  ← 分类头输入是 Prefix 的 hidden delta
orth_every_n_steps = 20, orth_start_step = 50
```

**训练过程（崩溃）**：
| epoch | gen_loss | orth_loss | cls_loss | valid PPL | test PPL |
|---|---|---|---|---|---|
| 1 | 13.90 | 0.00024 | 1.87 | 4,766,439 | 5,087,453 |
| 2 | 17.26 | 0.00005 | 0.59 | 5,651,830 | 13,860,334 |

gen_loss 从正常的 ~1.2 爆炸到 13.9（epoch 1），继续恶化到 17.3（epoch 2）。PPL 达到百万级——模型完全崩溃。

**根因定位（代码审计）**：

`src/losses.py` 中的 `compute_delta_prefix_hidden()` 函数：

```python
# 第一个前向（base-only）：✅ 正确包在 torch.no_grad() 中
with torch.no_grad():
    with lora_disabled_ctx(...):
        ...  # h_base 被 detach

# 第二个前向（prefix-only）：❌ 缺少 torch.no_grad()
with lora_disabled_ctx(...):       ← 没有 torch.no_grad()！
    ...  # h_prefix 仍在计算图中，有梯度！
```

**后果**：非 orth step（每 20 步中的 19 步）上，cls_loss 的梯度通过 prefix-only 前向路径（无 LoRA）传到 prefix_bank，而 gen_loss 梯度通过 both-on 前向路径（有 LoRA）也传到 prefix_bank。两股梯度来自**不同的前向计算图**（一个经过 LoRA、一个不经过），对 prefix_bank 的优化方向相互矛盾。prefix_bank 被推离适合生成的方向 → gen_loss 爆炸。

**后续修复**：第二个前向也包入 `torch.no_grad()`，使 `compute_delta_prefix_hidden` 完全 detach。修复后的代码用于所有后续实验（B6_fix 系列及之后）。

---

### 3.6 B6_fix：修复 bug 后的 DeST-RS 完整版（B6_fix 系列基准）

**实验目的**：验证修复 B6 的梯度泄漏 bug 后，完整 DeST-RS（orth + cls）能否正常训练并提升策略控制。

**修复内容**：
1. `compute_delta_prefix_hidden` 两个前向均包 `torch.no_grad()`
2. `orth_every_n_steps=1`（每步都算 orth + cls，而非每 20 步）
3. `orth_start_step=0`（从第一步开始）
4. `λ_orth=0.1`（原来是 0.05）
5. `λ_cls=1.0`（原来是 0.2）

每个 orth step 执行 4 次前向：
1. base-only（no_grad, LoRA off, Prefix off）→ h_base
2. prefix-only（LoRA off, Prefix on, 有梯度）→ h_prefix
3. lora-only（LoRA on, Prefix off, 有梯度）→ h_lora
4. both-on（LoRA on, Prefix on, 有梯度）→ gen_loss

orth_loss 从 h_prefix-h_base 和 h_lora-h_base 计算，cls_loss 从 delta_prefix 计算。

**训练过程**：
| epoch | gen_loss | orth_loss | cls_loss | valid PPL | test PPL |
|---|---|---|---|---|---|
| 1 | 1.45 | 0.0140 | 0.0362 | 7.97 | 8.29 |
| 2 | 1.18 | 0.0073 | 0.0096 | 8.30 | 8.47 |

训练恢复了稳定。gen_loss 回到正常的 1.18-1.45 范围。

**策略评估**：9 类准确率 = **31.85%**。

**结论**：虽然训练稳定了，但策略准确率（31.85%）**比 B4 的 41.85% 还差 10pp**。修复了崩溃，但没有修复"策略控制退化"的根本问题。orth + cls 没有起到预期的作用。

---

### 3.7 B6_cls_0.5 / B6_cls_2.0 / B6_cls_5.0：λ_cls 扫描

**实验目的**：验证策略分类监督权重（λ_cls）是否是关键因素——增大 cls_loss 权重能否改善策略控制。

**配置差异**（与 B6_fix 相比）：
| 实验 | λ_cls |
|---|---|
| B6_cls_0.5 | 0.5 |
| B6_cls_2.0 | 2.0 |
| B6_cls_5.0 | 5.0 |

其他参数同 B6_fix：λ_orth=0.1, orth_every_n_steps=1, cls_target=delta_prefix。

**结果**：
| 实验 | valid PPL | test PPL | 9 类 Acc | epoch 1 cls_loss | epoch 2 cls_loss |
|---|---|---|---|---|---|
| B6_cls_0.5 | 7.84 | 8.15 | 37.17% | 0.040 | 0.007 |
| B6_cls_2.0 | 7.88 | 8.05 | 33.33% | 0.034 | 0.0002 |
| B6_cls_5.0 | 7.92 | 8.03 | 39.63% | 0.055 | 0.005 |

cls_loss 在所有设置中都快速衰减到接近 0（epoch 2 时 0.0002-0.007）。分类头很快学会从 delta_prefix 识别策略（cls_loss 低），但**生成文本的策略准确率没有同步提升**。

**关键发现**：
- 分类头学的是"内部表示分类"——它能从 hidden state 区分策略，但这个信号没有有效传递到输出 token 分布
- λ_cls 从 0.5 扫到 5.0（10 倍范围），准确率在 33-40% 波动，最佳 39.63%（B6_cls_5.0）仍低于 B4（41.85%）
- **cls_loss 的监督信号本质上是稀疏的**（每个样本 1 个标量），而 gen_loss 在每个 token 上给信号（每个样本 ~200 个 token）。信号密度差 200 倍。

---

### 3.8 B6_orth_0.5 / B6_orth_1.0：λ_orth 扫描

**实验目的**：验证正交约束权重（λ_orth）是否是关键因素。B5 的 orth_loss 太小（~0.001），增大权重能否改善 Prefix 和 LoRA 的解耦。

**配置差异**：
| 实验 | λ_orth |
|---|---|
| B6_orth_0.5 | 0.5 |
| B6_orth_1.0 | 1.0 |

λ_cls=1.0 不变。

**结果**：
| 实验 | valid PPL | test PPL | 9 类 Acc | epoch 1 orth_loss | epoch 2 orth_loss |
|---|---|---|---|---|---|
| B6_orth_0.5 | 8.05 | 8.33 | 40.00% | 0.0072 | 0.0028 |
| B6_orth_1.0 | 8.14 | 8.36 | 40.37% | 0.0033 | 0.0003 |

**关键发现**：
1. orth_loss 在 epoch 1 已经很低（0.007-0.003），epoch 2 进一步降到 0.003-0.0003
2. **模型学会了绕过正交约束**——它没有真正让 Prefix 和 LoRA 的 hidden delta 正交，而是找到了一个让 cosine similarity 平方项很小的"几何捷径"
3. 加大 λ_orth 反而推高了 PPL（7.97 → 8.05 → 8.14），因为正交约束和生成目标在对抗
4. 最好的结果（B6_orth_1.0: 40.37%）仍不如 B4（41.85%）
5. **几何正交 ≠ 功能解耦：** 即使 hidden delta 在几何上被约束为"看起来正交"，LoRA 仍然在下游功能层面中和 Prefix 的策略信号

---

### 3.9 B6_lora_r8：降低 LoRA 强度

**实验目的**：假设 LoRA 的表示容量过大（r=16），导致它"过度补偿"Prefix 的策略信号。降低 LoRA 秩（r=8）看是否能减少干扰。

**配置差异**：
```
lora_r = 8
lora_alpha = 16（原来是 32）
λ_orth = 0.1, λ_cls = 1.0
```

**结果**：
| epoch | gen_loss | valid PPL | test PPL | 9 类 Acc |
|---|---|---|---|---|
| 1 | 1.46 | 8.21 | 8.45 | — |
| 2 | 1.22 | 7.93 | 8.03 | **28.52%** |

28.52% 是所有 B6 变体中最低的（除了崩溃的）。**降低 LoRA 容量让策略控制变得更差，而不是更好。**

这可能是因为：LoRA 容量小了之后，反而学得更"窄"——集中在高频生成模式上，进一步压低了 Prefix 策略差异的表达空间。

---

### 3.10 B7：warm-start DeST-RS 及扩展训练

**实验目的**：从 B3 训好的 Prefix 初始化，再做联合训练，验证"好的 Prefix 起点"能否改善策略控制。

**原始 B7（旧代码，1 epoch）**：
```
warm_start_dir → B3 的 prefix_bank.pt
adapter_mode = "dest_rs"
所有模块可训练，联合训练 1 epoch
```

| epoch | gen_loss | orth_loss | cls_loss | valid PPL | test PPL | 9 类 Acc |
|---|---|---|---|---|---|---|
| 1 | 1.38 | 0.029 | 0.009 | 8.02 | 8.18 | **45.56%** |

45.56% 非常接近 B3（48.69%），PPL 从 10.56 降到 8.02。这是**所有联合训练方案中最好的结果**。

**B7_ep2 / B7_ep3（新代码修复后，扩展训练）**：
| 实验 | epoch 1 PPL | epoch 2 PPL | epoch 3 PPL | 9 类 Acc |
|---|---|---|---|---|
| B7_ep2 | 8.02 | 8.34 | — | 45.56% |
| B7_ep3 | 8.02 | 8.34 | 9.48 | 45.56% |

扩展训练没有改善——best checkpoint 始终在 epoch 1，epoch 2 和 3 都过拟合（PPL 上升）。说明 warm-start Prefix 在 1 epoch 后就开始被 gen_loss 拉扯退化。

B7 的成功关键在于：**Prefix 已经有了好的策略表征（来自 B3），1 epoch 的温和训练不足以破坏它。**

---

### 3.11 B6_grad：梯度路由（路线 A）

**实验目的**：假设 naive 联合训练的失败来自**梯度冲突**——gen_loss 和 cls_loss 对 prefix_bank 的优化方向相反。通过梯度路由消除冲突：gen_loss 梯度只到 LoRA，cls_loss 梯度只到 Prefix。

**原理**：
```
原版:                           路线 A (grad_routing=True):
gen_loss → prefix_bank + LoRA   gen_loss → LoRA only (detach_prefix=True)
cls_loss → prefix_bank          cls_loss → prefix_bank + classifier
orth_loss → prefix_bank + LoRA  orth_loss → prefix_bank + LoRA (各自 detach)
```

**实现**：
- `modeling.py`：`HybridStrategyModel.forward()` 新增 `detach_prefix` 参数。设为 True 时，prefix embeddings 在拼接到 input_embeds 前 detach。
- `losses.py`：orth step 的 pass 4（both-on gen_loss 前向）自动传 `detach_prefix=True`。非 orth step 中新增 `compute_delta_prefix_for_grad_routing()` 函数——prefix-only 前向**不包 no_grad**，使 cls_loss 梯度可到 prefix_bank。
- `config.py`：新增 `grad_routing: bool = False`。

**物理含义**：gen_loss 计算时 prefix 参与前向（提供策略信息），但梯度不回传——梯度只更新 LoRA。Prefix 通过 cls_loss 和 orth_loss 单独学习，不被 gen_loss 干扰。

**B6_grad 结果（完整梯度路由，λ_orth=0.1, λ_cls=1.0）**：
| epoch | gen_loss | orth_loss | cls_loss | valid PPL | test PPL | 9 类 Acc |
|---|---|---|---|---|---|---|
| 1 | 1.43 | 0.0065 | 0.022 | 7.91 | 8.05 | **37.78%** |
| 2 | 1.18 | 0.0017 | 0.004 | 8.28 | 8.48 | — |

**对比**：
- vs B6_fix（31.85%）：**+5.9pp**，梯度路由确实有帮助
- vs B4（41.85%）：仍低 4pp
- vs B3（48.69%）：仍低 11pp

**结论**：梯度冲突**是问题的一部分（贡献约 6pp），但不是全部**。消除梯度冲突后仍有 11pp 的差距。

**B6_grad_no_orth（梯度路由，无 orth）**：
| epoch | gen_loss | cls_loss | valid PPL | test PPL | 9 类 Acc |
|---|---|---|---|---|---|
| 1 | 7.99 | 3.13 | 8803 | 7517 | **6.30%** |
| 2 | 14.21 | 2.58 | 67,634,467 | 70,907,331 | — |

**崩溃分析**：λ_orth=0 → should_compute_orth=False → 进入非 orth 路径。在非 orth 路径中，梯度路由使：
- gen_loss → LoRA only
- cls_loss → prefix_bank only

但没有 orth_loss 提供协调。两个模块各自独立优化——Prefix 往 cls_loss 方向跑，LoRA 往 gen_loss 方向跑。两者的 hidden delta 方向快速发散，gen_loss 在 epoch 1 就达到 7.99（正常 ~1.4），epoch 2 继续恶化。

**教训**：orth_loss 在梯度路由中**不是可选的**——它是 Prefix 和 LoRA 之间唯一的协调信号。去掉它后，两个模块立刻各自为政。

---

### 3.12 B6_contrastive / B6_contrastive_no_orth：对比生成损失（方法 1）

**实验目的**：cls_loss 的信号太稀疏（每个样本 1 个标量）。改用对比生成损失：正确策略 Prefix 的 gen_loss 应该低于错误策略 Prefix 的 gen_loss。

**原理**：
```python
# 正确策略前向 → gen_loss_right
# 随机选一个错误策略 → gen_loss_wrong（no_grad）
contrastive_loss = max(0, gen_loss_right - gen_loss_wrong + margin)
# margin = 0.1
```

**为什么理论上更好**：对比信号在每个 token 上生效（gen_loss 是 per-token loss 的均值），信号密度和 gen_loss 一样。而且直接优化"正确策略 → 更好生成"，与评估目标一致。

**实现**：
- `config.py`：新增 `contrastive_loss`, `lambda_contrastive`, `contrastive_margin`
- `losses.py`：新增 `sample_wrong_strategies()`（每个样本随机选不同策略）、`compute_contrastive_loss()`（额外一次 no_grad 前向）
- `train.py`：新增 contrastive_loss 追踪

**B6_contrastive（contrastive + orth, λ_contrastive=1.0, margin=0.1）**：
| epoch | gen_loss | orth_loss | contrastive_loss | valid PPL | test PPL | 9 类 Acc |
|---|---|---|---|---|---|---|
| 1 | 1.53 | 0.021 | 0.000 | 8.27 | 8.38 | — |
| 2 | 1.20 | 0.013 | 0.000 | 8.06 | 8.28 | **42.59%** |

**B6_contrastive_no_orth（contrastive，无 orth）**：
| epoch | gen_loss | contrastive_loss | valid PPL | test PPL | 9 类 Acc |
|---|---|---|---|---|---|
| 1 | 1.44 | 0.000 | 7.90 | 8.15 | **41.11%** |
| 2 | 1.19 | 0.000 | 8.05 | 8.36 | — |

**关键发现**：contrastive_loss **在两个实验中均为 0.000**。margin（0.1）从未被触发。

> 换一个错误策略的 Prefix，模型的 gen_loss 没有显著变化（差异 < 0.1）。gen_loss 在 token 级别天然不区分策略——它只关心"下一个 token 该是什么"，而大部分 token 与策略无关。

**B6_contrastive vs B6_contrastive_no_orth**：加 orth 的版本（42.59%）比不加（41.11%）略好 1.5pp。本质上退化为 Prefix+LoRA+Orth（B5 的改进版）。orth_every_n_steps=1 和 orth_start_step=0 让 orth 更有效。

---

### 3.13 B9：Prefix→LoRA（两阶段训练，新定义）

**实验目的**：经过前面的排除分析（非梯度冲突、非损失设计、非联合训练污染），假设问题的根因是 **LoRA 改变了 Prefix 运行的环境**。B9 验证：如果加载 B3 训好的 Prefix 并完全冻结，只训 LoRA，策略控制能否完整保留。

**配置差异**：
```
adapter_mode = "prefix_lora"
enable_prefix = True          ← 推理时 Prefix 启用
enable_lora = True            ← 推理时 LoRA 启用
train_prefix = False          ← 不更新 Prefix
train_lora = True             ← 只更新 LoRA
train_classifier = False      ← 不训练分类头
freeze_prefix = True
warm_start_dir = "output/other/casino_augmented/b3_prefix_only"
λ_orth = 0, λ_cls = 0        ← 不使用任何辅助损失
```

**生成时的行为**：Prefix（B3 训好的，冻结）+ LoRA（新训的）同时参与前向。Prefix 提供策略信号，LoRA 改善生成质量。

**训练过程**：
| epoch | gen_loss | valid PPL | test PPL |
|---|---|---|---|
| 1 | 1.37 | 8.06 | 8.19 |
| 2 | 1.16 | 8.36 | 8.52 |

**策略评估**：9 类准确率 = **44.44%**。

**逐策略 breakdown（B9 vs B3）**：
| 策略 | B3 Acc | B9 Acc | 变化 | 含义 |
|---|---|---|---|---|
| elicit-pref | 60.0% | 70.0% | ↑ +10pp | LoRA 增强了这个策略 |
| self-need | 70.0% | 83.3% | ↑ +13pp | LoRA 增强了这个策略 |
| small-talk | 40.0% | 50.0% | ↑ +10pp | 轻微改善 |
| vouch-fair | 23.3% | 23.3% | = | 不变 |
| promote-coordination | 23.3% | 23.3% | = | 不变 |
| no-need | 96.7% | 93.3% | ↓ -3pp | 轻微退化 |
| showing-empathy | 70.0% | 53.3% | ↓ -17pp | LoRA 严重破坏 |
| other-need | 26.7% | **0.0%** | ↓ 归零 | 被 LoRA 彻底抹掉 |
| uv-part | 25.9% | **3.3%** | ↓ 接近归零 | 被 LoRA 几乎抹掉 |

**关键发现**：
- B9 的 Prefix 和 B3 的 Prefix 是**完全相同的权重**（冻结不动）。理论上应同样拿到 48.7%。实际只有 44.4%，掉了 4.3pp。
- 5 个策略改善或不变，4 个策略退化——其中 other-need 和 uv-part 被完全抹掉
- B7（warm-start + 1 epoch 轻微训练 = 45.6%）比 B9（完全冻结 = 44.4%）还高 1pp

**这说明什么**：

> LoRA 的介入改变了模型内部的 Q/K/V/O 注意力投影。B3 的 Prefix 是在**纯基座模型**（无 LoRA）环境下训练的——它学会了如何在这个"干净"的 attention 空间中施加策略影响。当推理时加上 LoRA（改变了 Q/K/V/O 的计算），Prefix 的策略信号经过不同的投影后到达输出，部分失效。**这不是训练层面的问题，是环境层面的不兼容。** Prefix 在一个模型中学到的表征，在另一个模型（哪怕只是加了 r=16 的 LoRA 补丁）中无法完整迁移。

B7 之所以比 B9 高 1pp，是因为 B7 在 1 epoch 的温和训练中，Prefix 做了微量调整以适应 LoRA 环境。

---

### 3.14 实验总结：排除分析链

从 B4 发现"加 LoRA 策略控制就退化"开始，我们通过一系列实验逐层排除根因：

```
B1（27.4%）：Frozen Base + 文本指令 → PEFT 确实有价值（B3 比 B1 高 21pp）
B2（11.1%）：LoRA only → 无 Prefix 就无法做策略控制
B3（48.7%）：Prefix only → 策略控制上界，PPL 代价高
B4（41.9%）：Prefix+LoRA 联合训练，策略控制退化 7pp
  ↓ 假设是正交约束不够？
B5（36.1%）：加正交约束 → 更差。orth_loss 太弱，被 gen_loss 压倒。
  ↓ 正交+分类监督是不是就行了？
B6（崩溃）：加 cls_loss → 训练崩溃。定位代码 bug→修复。
  ↓ 修复后调参
B6_fix（31.9%）：修复 bug → 训练稳定，但准确率最低。
  ↓ 扫 λ_cls（0.5-5.0）和 λ_orth（0.1-1.0）
B6_cls_5.0（39.6%）/ B6_orth_1.0（40.4%）：超参扫描天花板 ~40%。
  ↓ 假设是梯度冲突：gen_loss 和 cls_loss 对 prefix 方向矛盾
B6_grad（37.8%）：梯度路由 → +6pp，但仍差 11pp。梯度冲突不是主因。
B6_grad_no_orth（崩溃）：去掉 orth → 两个模块立刻发散。orth 是协调必需品。
  ↓ 假设是 cls_loss 信号太稀疏，改用稠密的对比信号
B6_contrastive（42.6%）：contrastive_loss 全程为零。gen_loss 不区分策略。
  ↓ 假设是联合训练中 LoRA 反向污染 Prefix
B9（44.4%）：冻结 B3 Prefix → 仍掉 4pp。联合训练不是原因。
  ↓
  ✅ 唯一剩下的解释：环境级不兼容。
     LoRA 改变了 attention 投影 → B3 的 Prefix 在新环境中失效。
```

每个"否"都是通过实验得出的，不是猜测。

---

## 四、评估器校准

### 4.1 方法

用 Qwen3-8B 对**人工标注的 ground truth 验证集回复**做策略分类，与人工标签对比。Few-shot 样本来自训练集（每策略 2 个）。

### 4.2 结果

- **总体准确率**：104/200 = **52.0%**
- **Null 预测**：0

**逐策略准确率**：

| 策略 | 准确率 |
|---|---|
| elicit-pref | 16/19 = 84.2% |
| small-talk | 41/71 = 57.7% |
| promote-coordination | 15/29 = 51.7% |
| self-need | 19/37 = 51.4% |
| showing-empathy | 4/10 = 40.0% |
| vouch-fair | 5/14 = 35.7% |
| other-need | 3/10 = 30.0% |
| uv-part | 1/4 = 25.0% |
| no-need | 0/6 = **0.0%** |

### 4.3 与模型结果对照

| 策略 | 评估器对人工文本 | B3 模型生成 | 解读 |
|---|---|---|---|
| no-need | **0%** | **96.7%** | 模型学会了生成"夸张版"拒绝，评估器能认但人工不会这么写 |
| other-need | 30% | 26.7% | 已达天花板 |
| uv-part | 25% | 25.9% | 已达天花板 |
| small-talk | 57.7% | 40% | 有提升空间但不大 |
| elicit-pref | 84.2% | 60% | 主要的可改进空间 |

### 4.4 主要混淆

```
small-talk → showing-empathy:  12 次
promote-coordination → self-need: 9 次
self-need → uv-part:            9 次
other-need → self-need:         5 次
small-talk → promote-coordination: 5 次
vouch-fair → self-need:         5 次
```

---

## 五、完整结果排名

| 排名 | 实验 | 9 类 Acc | test PPL | 占天花板 |
|---|---|---|---|---|
| 🥇 | B3 Prefix-only | **48.69%** | 11.10 | 93.6% |
| 🥈 | B7 warm-start | 45.56% | 8.18 | 87.6% |
| 🥉 | B9 Prefix→LoRA | 44.44% | 8.19 | 85.5% |
| 4 | B6_contrastive | 42.59% | 8.28 | 81.9% |
| 5 | B4 Prefix+LoRA | 41.85% | 8.39 | 80.5% |
| 6 | B6_contrastive_no_orth | 41.11% | 8.15 | 79.1% |
| 7 | B6_orth_1.0 | 40.37% | 8.36 | 77.6% |
| 8 | B6_cls_5.0 | 39.63% | 8.03 | 76.2% |
| 9 | B6_grad | 37.78% | 8.05 | 72.7% |
| 10 | B5 +Orth | 36.06% | 8.00 | 69.3% |
| 11 | B6_fix | 31.85% | 8.29 | 61.3% |
| 12 | **B1 Frozen Base** | **27.41%** | — | 52.7% |
| 13 | B2 LoRA-only | 11.11% | 8.33 | 21.4% |

**规律**：
- 策略准确率越高 → PPL 越高，策略控制与生成质量之间存在系统性 trade-off
- B1（27.4%）确认了 PEFT 的正向价值：B3 的 Prefix 训练将准确率从 27.4% 提升至 48.7%（+21pp）
- B2（11.1%）证明无 Prefix 就无策略控制——LoRA 自身无法接收策略 ID

---

## 六、创新点

### 创新点 1：首次通过系统排除分析法，揭示混合 PEFT 中 LoRA 对 Prefix 策略控制的破坏是"环境级不兼容"

**问题**：Prefix+LoRA 联合训练后策略控制退化（B4 vs B3 差 7pp），根因一直不明确。

**我们的做法**：设计了三组"排除性实验"，逐一关闭可能的干扰源：

| 排除目标 | 实验 | 设计 | 结果 | 结论 |
|---|---|---|---|---|
| 梯度冲突 | B6_grad | 梯度路由：gen_loss 梯度不到 Prefix | 37.78%（+6pp，但仍有 11pp 差距）| 梯度冲突**不是**主因 |
| 损失设计 | B6_contrastive | 用稠密对比 gen_loss 替代稀疏 cls_loss | contrastive_loss=0 全程未触发 | gen_loss 天然不区分策略，损失设计**不是**主因 |
| 联合训练污染 | B9 | 冻结 B3 的 Prefix，只训 LoRA | 44.44%（Prefix 完全不动，仍掉 4pp）| 联合训练**不是**主因 |

**排除后的唯一解释**：LoRA 改变了模型内部的 Q/K/V/O 注意力投影。B3 的 Prefix 是在纯基座模型环境中学习的策略表征，当 LoRA 介入后，注意力计算的环境变了，Prefix 的策略信号在新环境中自适应失效。**这不是训练层面的冲突，是模型架构环境层面的不兼容。**

**支撑实验量**：11 组消融实验（B2-B9 + fix + grad + contrastive）。

**与已有工作的关系**：Kim et al.（EMNLP 2024 Findings）通过 SVD 分析发现 LoRA 会扭曲预训练表示空间（有效秩降至 ~60%），而 Prefix Tuning 保留了完整的表示空间。他们提出顺序组合（Prefix → LoRA）。我们的工作在此基础上更进一步：（1）通过冻结 Prefix 实验（B9）首次证明即使 Prefix 完全不动，LoRA 的介入仍会破坏策略控制，从"表示空间扭曲"推进到"环境级不兼容"；（2）通过梯度路由和对比损失实验排除了梯度冲突和损失设计的干扰；（3）首次对 LLM 评估器进行了系统校准。

---

### 创新点 2：首次对 LLM-based 策略评估器进行系统校准，揭示任务真实天花板

**问题**：已有工作用 LLM 做策略评估，但从未有人校准过评估器自身的准确率。

**我们的做法**：用人工标注的 ground truth 验证集回复（200 条），让同一个 LLM 评估器（Qwen3-8B）做策略分类，与人工标签对比。

**关键发现**：

| 发现 | 数据 | 意义 |
|---|---|---|
| 评估器总体天花板 | 52.0% | 任务存在 48% 的不可消除噪声 |
| B3 占天花板比例 | 93.6% | 当前最好的方法已接近理论上限 |
| no-need 人工文本准确率 | 0% | 但模型生成的 no-need 评估器判对 96.7%——这是 shortcut learning |
| 评估器 self-need 偏见 | 36-38% | 评估器对任何输入都有 ~37% 概率判 self-need |
| 低频策略天花板 | other-need 30%, uv-part 25% | 这些策略在评估层面就不可靠 |

**方法论贡献**：任何使用 LLM-as-judge 做策略评估的工作，都应该先校准评估器。我们提供了校准方法和 baseline 数据。

---

### 创新点 3：基于分析发现，提出两步训练方案（B9/B7）实现策略控制与生成质量的最优折中

**方案**：
- Stage 1：独立训练 Prefix（B3），专注策略控制学习
- Stage 2：冻结 Prefix，训练 LoRA（B9），专注生成适配

**效果**：
- 策略准确率：44.44%，保留了 B3 的 85.5%
- 生成 PPL：8.06，相比 B3（10.56）降低 24%

**创新的本质**：两步训练本身不是新方法（Kim et al. 也提出了类似的顺序组合）。但我们的贡献在于——**通过创新点 1 的系统排除分析，证明了为什么在这个具体任务上当前条件下，两步训练是唯一有效的方案**。B9 不是"我们发明的方法"，而是"我们通过排除法收敛到的必然解"。这种"分析驱动设计"的范式，比直接提出一个方法更有学术深度。

---

## 七、关键教训

### 7.1 B6 崩溃（原始代码 bug）
- `compute_delta_prefix_hidden` 中第二个前向缺 `torch.no_grad()`
- 非 orth step 上 cls_loss 梯度通过错误路径传到 prefix_bank，gen_loss 从 1.2 爆炸到 13.9
- 修复后所有实验基于新代码

### 7.2 B6_grad_no_orth 崩溃
- 无 orth 协调时，梯度路由下 Prefix 和 LoRA 各自发散
- 证明 orth 不是"解耦工具"而是"兜底协调"

### 7.3 对比损失全程为零
- 换错误策略 Prefix 不会增加 gen_loss
- gen_loss 信号天然不区分策略——大部分 token 策略无关

### 7.4 评估器 self-need 偏见与 shortcut learning
- 评估器对任何输入都有 36-38% 概率判 self-need
- no-need 的 96.7% 是虚假的——评估器对人工 no-need 准确率为 0%，但模型学会了生成"夸张拒绝"
- 低频策略（other-need, uv-part）根本没有可靠的评估信号

### 7.5 B1 baseline 的 thinking mode 问题
- Qwen3-8B 收到文本策略指令后，生成时自动输出 `<think>...</think>` 推理过程，其中包含策略名称
- 评估器看到 `<think>` 内的策略描述会直接判对，造成策略泄露
- 尝试了 `enable_thinking=False`、`model.generation_config`、逐 token 生成等方式均无法彻底关闭
- 最终在生成后手工 strip `<think>` 标签解决
- 教训：使用 Qwen3 做文本指令控制的 baseline 时，必须验证"模型不是在作弊"

---

## 八、论文叙事建议

```
Abstract
├── 问题：谈判策略可控生成中，混合 PEFT 的 Prefix 和 LoRA 能否协同？
├── 方法：11 组消融实验 + 梯度路由 + 对比损失 + 评估器校准
├── 发现：
│   ├── LoRA 在任何配置下都会破坏 Prefix 的策略控制（环境级不兼容）
│   ├── LLM 评估器天花板仅 52%，此前所有工作缺少校准
│   └── 两步训练（冻结 Prefix + 训 LoRA）是当前最优解
└── 贡献：第一次系统揭示混合 PEFT 在策略控制中的功能边界

1. Introduction     — 谈判策略可控生成 + 混合 PEFT 的未探索问题
2. Related Work     — PEFT / 可控生成 / 谈判对话 / LLM 评估
3. Method           — Prefix + LoRA + Orth + Cls + 各变体定义
4. Experiments
   4.1 实验设置：数据、模型、评估方式
   4.2 主消融（B2-B6）：功能分工与干扰
   4.3 梯度路由（B6_grad）：排除梯度冲突
   4.4 对比损失（B6_contrastive）：排除损失设计
   4.5 冻结 Prefix（B9）：定位环境级不兼容
   4.6 评估器校准：天花板分析
5. Discussion       — 为什么 LoRA 破坏 Prefix / 天花板的意义
6. Conclusion       — 总结 + 未来方向
```

---

## 九、文件索引

| 文件 | 内容 |
|---|---|
| `src/losses.py` | 所有 loss 函数，含梯度路由 + 对比损失 |
| `src/modeling.py` | HybridStrategyModel + detach_prefix |
| `src/config.py` | TrainConfig，含所有实验参数 |
| `train.py` | 训练主循环 |
| `scripts/evaluate_strategy_control_llm.py` | LLM 策略评估器 |
| `scripts/calibrate_evaluator.py` | 评估器校准脚本 |
| `configs/b2_lora_only.json` ~ `configs/b9_prefix_then_lora.json` | 所有实验配置 |
| `run_grad_routing.sh` | 梯度路由实验（串行）|
| `run_grad_parallel.sh` | 梯度路由实验（双卡并行）|
| `scripts/generate_b1_swap.py` | B1 frozen base swap 生成脚本 |
| `run_method1_b9.sh` | 方法 1 + B9 并行脚本 |
| `output/need/casino_augmented/` | 原始 B2-B9 结果 |
| `output/need/casino_augmented_fix_b6/` | B1 + B6_fix + grad routing + contrastive + B9 结果 |
